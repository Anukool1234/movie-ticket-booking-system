package com.movieticket.entity.enums;

/**
 * Application roles used for role-based access control (RBAC).
 * ADMIN manages catalog and policies; CUSTOMER browses and books tickets.
 */
public enum Role {
    ADMIN,
    CUSTOMER
}
